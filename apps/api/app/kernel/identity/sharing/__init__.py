"""M5-5 沉淀同步与团队共享。

把 M4 的本地沉淀（历史 / 收藏 / 清单 / 报告）同步到服务端按 owner 归属的存储，
并支持团队内显式共享。本包是「跨用户数据可见性的引入点」，最高隐私风险，因此：

- 同步只上送元数据 / 引用 / 来源锚点 / 用户自填短字段，**绝不上送正文或原始案情**。
- 同步默认 owner 私有（team_id=None / visibility=private）。
- 共享必须显式动作，默认 visibility=private；共享后受 M5-4 权限模型约束。
- 案例侧 AI 内容（报告等）必须可追溯 case_id + source_chunk_id；无锚点不进入共享。
- ENABLE_TEAM_SHARING 默认 false：关闭即回到 M4 本地沉淀末态（单用户、纯前端、不上送）。
"""
