"""M5-3 团队空间与数据隔离。

引入 team_id / workspace_id，把服务端持久化的沉淀对象（收藏 / 清单 / 报告引用）
按团队（或单用户 owner）做行级强隔离：跨团队、跨用户默认不可见。

安全基调（M5-1 合同 / 落地基调）：
- 隔离默认单租户私有（single_tenant_private）：team_id 为空时等同当前单用户私有行为。
- 所有查询层强制带租户过滤；隔离不彻底（团队间数据串读）即 NO_GO。
- 持久层只存元数据 / 引用 / 结构化关系字段，绝不存正文。
- ENABLE_TEAM_WORKSPACE 默认 false：关闭时本模块不建表、不写入，回到 M5-2 / M4 末态。
"""
