# M1.3x-7 灰度候选 readiness 复核

- Step: `M1.3x-7`
- Generated at: `2026-06-10T21:04:11+08:00`
- Scope: 只复核 `M1.3x-6 selected candidate` 是否允许成为受控灰度候选；不默认开启，不对外放量，不进入 M2。
- Conclusion: `NO_GO`
- Default feature flag requirement: `ENABLE_WEIGHTED_RERANK=false`，必须保持默认关闭。

## 读取范围

| 类别 | 文件 |
| --- | --- |
| 分步骤文档 | `落地设计文档/11-M1.3x回归清零版分步骤文档.md` |
| M1.3x-6 参数定版产物 | `docs/development/m1.3x-candidate-comparison-20260610-200044.md`, `docs/development/m1.3x-candidate-comparison-20260610-200044.json`, `docs/development/m1.3x-parameter-version-20260610-200044.json` |
| 先前灰度复核参考 | `docs/development/m1.3-gray-candidate-readiness-20260610-150256.md`, `docs/development/m1.3-gray-candidate-readiness-20260610-150256.json` |
| 后端配置与接口 | `apps/api/app/core/config.py`, `apps/api/app/api/search.py`, `apps/api/app/api/health.py` |
| 前端调用与页面 | `apps/web/src/services/searchApi.ts`, `apps/web/src/pages/SearchPage.tsx` |
| 验证脚本 | `apps/api/scripts/day3_7_3_performance_smoke.py`, `apps/api/scripts/day3_7_5_rollback_drill.py`, `apps/api/scripts/m1_3_candidate_comparison.py`, `apps/api/scripts/m1_2_regression.py`, `apps/api/app/eval/product_eval.py` |

## Selected Candidate 结论

`M1.3x-6` 定版产物明确：

- `selectedCandidateExists=false`
- `selectedCandidateId=null`
- `selectedStatus=NO_GO`
- `allCandidatesNoGo=true`

因此本步骤按验收标准直接 `NO_GO`。后续所有 readiness 复核只作为稳定性与回滚证据，不能把不存在的 selected candidate 包装成“可灰度”。

## Feature Flags 当前状态

| 来源 | `ENABLE_WEIGHTED_RERANK` | 结果 |
| --- | --- | --- |
| `apps/api/app/core/config.py` 默认值 | `false` | 通过 |
| 运行时环境变量 | 未设置 | 通过 |
| `.env` | 未覆盖该项 | 通过 |
| `.env.example` | `false` | 通过 |
| `/health.feature_flags` | `false` | 通过 |

`/health.feature_flags` 复核结果：

```json
{
  "ENABLE_QUERY_REWRITE": false,
  "ENABLE_WEIGHTED_RERANK": false,
  "ENABLE_SUMMARY": false,
  "ENABLE_EXPANDED_SEARCH": false
}
```

结论：`ENABLE_WEIGHTED_RERANK` 默认状态仍为 `false`，本步骤未改变。

## Baseline / Candidate 路径兼容

检查方式：FastAPI `TestClient` + 脱敏 mock 依赖，分别验证默认 baseline 路径与受控 candidate 探针路径。该探针只验证接口兼容，不代表存在授权 selected candidate。

| 项 | Baseline path | Candidate path probe |
| --- | --- | --- |
| HTTP | `200` | `200` |
| `score_mode` | `base_retrieval` | `weighted_rerank` |
| `weighted_rerank_enabled` | `false` | `true` |
| `final_score == retrieval_score` | `true` | `false` |
| 顶层 schema | 稳定 | 稳定 |
| 结果项 schema | 稳定 | 稳定 |

schema 复核结果：

- 顶层字段集合一致，字段数 `7`。
- 结果项字段集合一致，字段数 `32`。
- 前端依赖字段类型检查通过。

结论：

1. baseline 路径继续可用。
2. candidate 路径可通过 `score_breakdown.score_mode` 与 `score_breakdown.weighted_rerank_enabled` 区分。
3. schema 兼容，前端调用契约未破坏。
4. 该 candidate 路径仅是受控探针，不可视为被批准的灰度候选。

## 复跑结果

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| backend tests | `cd apps/api && pytest` | `189 passed in 19.88s`；有 1 条非阻断 `pytest-asyncio` 配置弃用告警 |
| frontend tests | `cd apps/web && npm test` | `4` files, `40` tests passed |
| frontend build | `cd apps/web && npm run build` | passed |
| product eval | `cd apps/api && python -m app.eval.product_eval --comparison-mode product_chain --current-weighted-rerank --out docs/development/m1.3x-gray-candidate-product-eval-20260610-210411.json --bad-cases-out docs/development/m1.3x-gray-candidate-product-bad-cases-20260610-210411.json` | current `P@5=0.048`, `NDCG@10=0.171`, `Top10=0.68`；`METRIC_REGRESSION=7`，`RECALL_MISS=7`；`weightedRerankGrayCandidate=false` |
| M1.3 candidate comparison | `cd apps/api && python scripts/m1_3_candidate_comparison.py --performance-smoke docs/development/m1.3x-gray-candidate-performance-smoke-20260610-210411.json --rollback-drill docs/development/m1.3x-gray-candidate-rollback-drill-20260610-210411.json --out-json docs/development/m1.3x-gray-candidate-comparison-runner-20260610-210411.json --out-md docs/development/m1.3x-gray-candidate-comparison-runner-20260610-210411.md --parameter-version-out docs/development/m1.3x-gray-candidate-comparison-runner-parameter-version-20260610-210411.json` | `selected_status=NO_GO`，`selected_candidate_id=null` |
| M1.2 runner compatibility | `cd apps/api && python scripts/m1_2_regression.py --comparison-mode product_chain --out docs/development/m1.3x-gray-candidate-m1.2-regression-20260610-210411.json --parameter-version-out docs/development/m1.3x-gray-candidate-m1.2-parameter-version-20260610-210411.json` | `currentAfter P@5=0.040, NDCG@10=0.1539, Top10=0.64`；`before->after REGRESSED=4`；`after-vs-baseline REGRESSED=9`；`METRIC_REGRESSION=9` |
| performance smoke | `cd apps/api && python scripts/day3_7_3_performance_smoke.py --out docs/development/m1.3x-gray-candidate-performance-smoke-20260610-210411.json` | `status_counts={"200":5}`，`error_rate=0.0`，warm `P95=454ms`，`warm_p95_under_3s=true` |
| rollback drill | `cd apps/api && python scripts/day3_7_5_rollback_drill.py --out docs/development/m1.3x-gray-candidate-rollback-drill-20260610-210411.json` | `status=passed`，max rollback `19ms`，weighted rerank rollback probe `12ms`，无需重建索引 |
| frontend no blank screen | Browser + Vite `http://127.0.0.1:5177/search` | `#root` child count `1`，主输入框存在，主按钮数 `2`，H1 存在，console error `0` |
| `/health.feature_flags` | FastAPI `TestClient GET /health` | 返回 `ENABLE_WEIGHTED_RERANK=false` |

## 硬门禁总结

| 门禁 | 结果 | 证据 |
| --- | --- | --- |
| `M1.3x-6` 有唯一 selected candidate | `FAIL` | `selectedCandidateExists=false`，`selectedCandidateId=null` |
| `ENABLE_WEIGHTED_RERANK` 默认仍为 `false` | `PASS` | config / env / `.env.example` / `/health` 一致 |
| baseline 路径继续可用 | `PASS` | `score_mode=base_retrieval`，HTTP `200` |
| candidate 探针路径 schema 兼容 | `PASS` | `score_mode=weighted_rerank`，字段集合一致 |
| product eval gray candidate hard gate | `FAIL` | `METRIC_REGRESSION=7`，`weightedRerankGrayCandidate=false` |
| M1.3 candidate comparison selected candidate hard gate | `FAIL` | `selected_status=NO_GO` |
| M1.2 compatibility runner 无回归 | `FAIL` | `before->after REGRESSED=4`，`after-vs-baseline REGRESSED=9`，`METRIC_REGRESSION=9` |
| warm P95 `<3s` | `PASS` | warm `P95=454ms` |
| rollback `<60s` 且无需重建索引 | `PASS` | max rollback `19ms`，no index rebuild |
| 前端不白屏 | `PASS` | root 渲染正常，console error `0` |
| API schema 稳定 | `PASS` | baseline / candidate probe 字段一致 |

总门禁结论：`NO_GO`。

## 启用门槛

只有全部满足以下条件，才允许在后续步骤重新讨论“成为受控灰度候选”：

1. 有且只有一个经授权参数定版产物给出 `selectedStatus=GO` 且 `selectedCandidateExists=true`。
2. `ENABLE_WEIGHTED_RERANK` 默认继续保持 `false`，只能在受控环境显式临时开启。
3. baseline 路径继续返回 `score_mode=base_retrieval`，并稳定可用。
4. candidate 路径返回 `score_mode=weighted_rerank`，且 schema 与 baseline 完全兼容。
5. product eval 满足：
   - `Precision@5 >= baseline`
   - `NDCG@10 > baseline`
   - `Top10 hit rate >= 0.60`
   - `METRIC_REGRESSION = 0`
6. M1.3 candidate comparison 满足：
   - `before->after REGRESSED = 0`
   - `after-vs-baseline REGRESSED = 0`
   - `grayCandidateHardGatePassed = true`
7. M1.2 compatibility runner 仍不出现新增回归。
8. performance smoke 满足 warm `P95 < 3000ms`，且无明显错误率恶化。
9. rollback drill 证明 `<60s`、无需重建索引。
10. 前端 `/search` 不白屏，能稳定消费 schema。
11. 所有产物与日志继续不写原始 query、案情正文、候选正文或 chunk 正文。

## 立即回退条件

如后续另有授权做内部受控灰度，任一条件触发即立刻回退到 baseline：

| 类别 | 条件 |
| --- | --- |
| 开关一致性 | `ENABLE_WEIGHTED_RERANK=false` 但 `/api/search` 返回 `score_mode=weighted_rerank` |
| schema | 顶层字段或前端依赖的结果项字段缺失、重命名或类型不兼容 |
| 稳定性 | 5xx 错误率 `>=1%` 持续 30 分钟 |
| 性能 | warm `P95 >= 3000ms` 或 smoke 出现明显恶化 |
| 回滚 | rollback drill 失败、恢复 `>=60s`、或需要重建索引 |
| 精准度 | `P@5 < baseline`、`NDCG@10 <= baseline`、或 `Top10 < 0.60` |
| 回归 | `before->after REGRESSED > 0`、`after-vs-baseline REGRESSED > 0`、或 `METRIC_REGRESSION > 0` |
| 前端 | `/search` 白屏、主输入框缺失、主按钮缺失、或出现阻断渲染错误 |
| 隐私 | 日志或产物出现原始 query、案情正文、候选正文或 chunk 正文 |

回退方式：

1. 设置或保持 `ENABLE_WEIGHTED_RERANK=false`。
2. 重新装载 API。
3. 验证 `/health.feature_flags.ENABLE_WEIGHTED_RERANK=false`。
4. 验证 `/api/search.results[].score_breakdown.score_mode=base_retrieval`。

本轮 drill 证明：weighted rerank 回退探针 `12ms`，整组最大回退耗时 `19ms`，无需重建索引。

## 边界与隐私确认

- 未新增排序修复。
- 未新增召回修复。
- 未修改 qrels。
- 未修改历史评测结果。
- 未删除坏样本。
- 未做 case-id hardcoding。
- 未做 query-id 特判。
- 未把 `ENABLE_WEIGHTED_RERANK` 默认改成 `true`。
- 未把“可成为灰度候选”偷换为“默认开启”或“允许进入 M2”。
- 本报告只写 query id、case id、rank、score、label、count、状态、reason code、runner/gate 名称、参数版本、性能/回滚摘要。

## 本步骤裁决

- `selected candidate exists`: `false`
- `ENABLE_WEIGHTED_RERANK default`: `false`
- `baseline path available`: `true`
- `candidate path schema compatible`: `true`
- `readiness status`: `NO_GO`

止损结论：

1. 继续关闭新排序。
2. 不进入 M2 readiness。
3. 只保留评测、日志、回归和受控实验能力。

下一步新会话标题：

`类案检索助手 M1.3x-8 验收与 M2 入口结论`
