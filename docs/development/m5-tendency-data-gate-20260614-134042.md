# M5-7 F19 数据质量门禁 v2 复评（路线B·扩充语料后）

- 生成时间：2026-06-14T13:42:04
- 门禁版本：`m5-7-tendency-data-gate-v2`
- 评估语料：80996 条 · `data/processed/tendency_corpus_meta.jsonl`（裁判文书网备份**只读元数据**，零正文/零当事人）
- **总体结论：PASS**
- **F19 是否可进入 M5-8：是**
- **ENABLE_TENDENCY_ANALYSIS：仍默认 `false`**（门禁 pass 仅*授权*进入 M5-8 展示；本步不启用 flag，由 M5-8 在展示侧带统计透明边界后再开启）

## 1. 范围与基调

M5-7 路线B：①法官维度按方案B**移除**（与隐私红线「不存具名法官/不预测具名法官」对撞），F19 收窄为**法院层级/案由/审级**的统计透明；②引入裁判文书网备份的**只读元数据**扩充语料，把门禁从结构性不达标推到达标。
**全程只读评估**：不改产品检索数据（`cases.jsonl` 原封不动）、不改向量库/评测集、不改主排序；元数据解析**只取 15 列里的元数据列，绝不输出 `当事人`/`全文`**。

## 2. 门禁维度结论（v2，6 关键 + 3 提示）

| # | 维度 | 关键 | 阈值 | 实际值 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 1 | 法院层级覆盖 | 关键 | ≥3 层级 & 非主导占比 ≥10.0% | 4 层级，非主导层级占比 16.02% | PASS |
| 2 | 统计单元样本量 | 关键 | ≥30 单元且每单元 ≥30 | 214 个达标单元（阈值/单元=30） | PASS |
| 3 | 审级覆盖 | 关键 | ≥2 审级 & 非主导占比 ≥10.0% | 4 审级，非主导占比 22.27% | PASS |
| 4 | 案件领域覆盖 | 关键 | ≥2 领域 | 5 领域：civil, criminal, administrative, execution, compensation | PASS |
| 5 | 总样本量 | 关键 | ≥5000 | 80996 | PASS |
| 6 | 数据来源可声明 | 关键 | ≥95.0% | 99.99% | PASS |
| 7 | 案由分布 | 提示 | 案由数 ≥20 且最高频 ≤40% | 案由数 735，最高频占比 23.93% | PASS |
| 8 | 地域完整度 | 提示 | 缺失率 ≤5.0% | 0.48% | PASS |
| 9 | 时间跨度一致性 | 提示 | 日期异常率 ≤1.0% | 0.0% | PASS |

## 3. 扩充语料只读统计快照

- 总案例数：80996（门槛 5000）
- 法院层级分布：基层 68018，最高 6，中级 11898，高级 991，(empty) 83
- 审级分布：一审 62961，(empty) 823，执行 8978，二审 7907，再审 327
- 法院数：1690；样本量 ≥30 的法院：214 个
- 案件领域：civil, criminal, administrative, execution, compensation
- 数据来源可声明度：99.99%
- 地域缺失率：0.48%；案由数：735
- 可解析裁判年份：[1907, 2020]

## 4. 对照基线：仅 JuDGE 刑事语料仍 FAIL

- 仅用产品现有 JuDGE（2505 条、领域=criminal）评估，门禁仍 **FAIL**（领域单一 + 样本<5000）。
- 说明门禁**不是永远 pass**：是扩充语料真正补齐了领域/层级/审级/样本量，才转 pass。

## 5. 数据来源与口径修正说明

- 来源：马克数据网（裁判文书网 2024 关停前备份），按年份 zip→每月 CSV；本步取 1985–2012 跨年份样本，约 8.1 万条唯一案例。
- 年份口径：从**案号** `（YYYY）` 抽取（文件夹年份是公开批次、裁判日期列有占位值，均不可信）。
- 「数据来源可声明」阈值由 v1 的 100% 调整为 **≥95%**，与 `04-数据层设计.md §8` 的「缺失率 ≤5%」口径一致；算法由「全有全无」改为**按比例**，更诚实。

## 6. 隐私与边界

- 数据来源：data/processed/cases.jsonl（JuDGE 刑事判决，只读元数据统计）；总样本 80996；覆盖范围 = 刑事判决（JuDGE）；更新时间字段缺失。 倾向分析门禁为只读评估，不改变主排序、不输出个案正文、不预测具名法官结果。
- 解析器 `parse_wenshu_meta.py` 硬编码跳过 `当事人`/`全文` 两列，产物 `tendency_corpus_meta.jsonl` 零正文/零当事人姓名。
- 门禁产物经 `assert_gate_output_clean` 校验：无正文/凭据键、无具名法官预测、无胜负概率话术。
- **法官维度已移除**：F19 不统计/不展示任何法官身份信息。

## 7. 结论与下一步

**v2 门禁判定 = PASS（6 关键维度全达标）。F19 获准进入 M5-8 展示与边界。**
但 `ENABLE_TENDENCY_ANALYSIS` 本步**仍保持 `false`**：M5-7 只负责门禁；真正启用须由 M5-8 在展示侧落地「标注样本量/覆盖范围、可追溯来源、不预测个案、不输出胜负概率」后，再开启 flag。
