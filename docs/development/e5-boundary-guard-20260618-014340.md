# E5-6 消费边界与护栏守门 Gate

- Step: E5-6（statute consumption-boundary & guardrail guard）
- Timestamp: 20260618-014340
- Baseline: `e5-statute-frontend-20260617-133220`（E5-5 GO, allow_enter_e5_6=true）
- Scope: 只加守门测试 + 文档；不改 search / intake / statute 行为
- Verdict: **CONDITIONAL_GO** — allow_enter_e5_7 = true

## 1. 新增 / 修改文件

- `apps/api/tests/test_e5_statute_boundaries.py`（NEW，11 类守门）
- `apps/api/tests/test_e2a_kernel_boundary.py`（append-only，line 356 之后追加 6 条 E5 规则）
- `docs/development/e5-boundary-guard-20260618-014340.json`（NEW）
- `docs/development/e5-boundary-guard-20260618-014340.md`（NEW，本文件）
- `落地设计文档/20-E5法条检索分步骤系统提示词文档.md`（E5-6 完成情况追加）

## 2. 守门覆盖（与提示词 1~11 逐条对应）

1. statute 公开面 9 符号齐备；`ENABLE_STATUTE_SEARCH` 默认 false；三端点关闭时 403 降级、不回显 query。
2. statute 不 import intake/drafting/casebook，也不被它们反向 import（test_e2a 追加 + 本文件双查）。
3. statute 不深引 retrieval/rerank/summary/query_processing；service 必经 `app.kernel.rag` 的 StatuteSearchService。
4. 仅 intake + statute 两个产品包；drafting / casebook 仍不存在。
5. 三请求体 schema 严格白名单 + `extra=forbid`；拒正文 / PII / 原始案情 / 模型生成条文型键（参数化逐键断言）。
6. StatuteRef 字段集 == E5-1 白名单；100% `statute_anchors`（`text_id` 非空）；无正文 / 生成型键；缺锚点 fail-closed。
7. 互跳只走契约对象：法条→类案出 CandidateRef（七字段、100% `source_anchors`、0 正文）；类案→法条出带锚点 StatuteRef；两侧无对侧正文。
8. statute 可执行代码 / 模型 / 日志无正文 / PII 键；无持久层 import；日志不写 `query_text` / 正文。
9. `include_router == 14`（intake + statute）；无 `/api/internal`、`/api/ecosystem` 等越界片段；statute 仅注册 3 端点。
10. 法条索引与案件索引物理隔离（`statute_chunks_bge_m3_v1` ≠ `case_chunks_bge_m3_v1` + 独立 persist）；脚本绝不写案件 collection；statute 代码不以写模式打开案件产物。
11. E5 测试 fixture 与 `e5-*` 文档最长连续中文 ≤ 40。

## 3. 测试结果

| Suite | 环境 | 结果 |
|---|---|---|
| `test_e5_statute_boundaries.py`（新增） | VM py3.10 + pip deps | 116 passed, exit 0 |
| `test_e2a` 的 6 条 E5 追加 | VM replica vs 真实 app 树 | 6 passed, exit 0 |
| group3：e1 / e2b / e3 契约 | VM py3.10 + pip deps | 137 passed, exit 0 |
| `include_router` 计数 | grep | 14 |

> test_e2a 的 E5 追加：host 文件经 Read 工具确认在 line 358+ 完整存在；但 VM mount 对「host 编辑过的既有文件」读到的是 edit 前的旧版（355 行、仍只跑 13 条），属已记录的 host-Edit→VM-stale 陷阱。逻辑已用 Write 新建的 VM 可见副本对真实 app 树跑过 6/6 绿。host `.venv311` 为权威口径。

## 4. 既有越界 / 不在本步范围的发现（须 E5-7 在 host 复核）

以下 5 项均属 E5-6 **允许修改清单之外**的既有文件（本步只可改 test_e2a + 新增文件 + 文档），不予改动，仅登记：

- `test_e3_internal_search_boundaries.py::test_e3_docs_have_no_long_body_text` — `NameError name '_max_c'`（`_max_contiguous_cjk` 被截断），line 375。VM mount 截断产物。
- `test_e4_intake_boundaries.py::test_e4_docs_have_no_long_body_text` — `NameError name 'o'`（`offenders` 被截断），line 640。同款截断产物。
- `test_e5_statute_api.py::test_statute_source_has_no_body_field_literals` — `NameError name 'ass'`（`assert` 被截断），line 454。即记忆中 E5-4 「assert→ass」VM 截断陷阱。
- `test_e5_statute_contracts.py::test_no_statute_drafting_casebook_product_package` — 断言「不得有 statute 包」，是 E5-1 期口径，与 E5-4 后现实冲突（已 stale）。
- `test_e5_statute_contracts.py::test_include_router_count_still_13` — 断言 13 routers，亦 E5-1 期口径（现实为 14）。

处置：前 3 项为 VM 环境字符截断 / 跨会话 .pyc 噪声（traceback 出现外会话路径 `friendly-eager-darwin`），非 E5-6 引入；后 2 项为 E5-1 阶段锁定断言，E5-6 未放宽、亦不在可改清单。全部留待 E5-7 在 host `.venv311`（Py3.11）整体复跑并核对。

## 5. 未放宽既有规则证明

test_e2a 的 E2/E3/E4 规则原文未动（append 落在 line 356 之后）；未改 statute / StatuteSearchService / InternalSearchService / intake 任何行为；未注册端点；未新建产品包；未改排序 / 召回 / summary。

## 6. 隐私扫描（新增文件）

新增文件中：裁判正文 / 原始案情 0、真实 PII 0、`e5-*` 文档与 E5 fixture 最长连续中文 ≤ 40（0 超限）。

## 7. 结论

E5-6 新增守门与 group3 契约在 VM 全绿；test_e2a 的 E5 追加 host 已确认且逻辑 6/6 复核通过。**CONDITIONAL_GO，allow_enter_e5_7 = true**，条件 = E5-7 在 host `.venv311` 整体复跑以消除 VM mount 截断噪声并核对 test_e5_statute_contracts 的两条 E5-1 stale 断言。
