# E4-5 消费边界与护栏守门 · 落地结论

- 步骤：E4-5（消费边界与护栏守门）
- 时间戳：20260617-025525
- 基线：e4-intake-frontend-20260617-020738（E4-4 GO）
- 结论：**GO**，allow_enter_e4_6 = true
- 是否改运行时行为：否（只加守门测试 + 文档）

## 一、新增 / 修改文件

新增：

- `apps/api/tests/test_e4_intake_boundaries.py`（44 用例，守门 1~10 全覆盖）
- `docs/development/e4-boundary-guard-20260617-025525.md` / `.json`

修改（append-only）：

- `apps/api/tests/test_e2a_kernel_boundary.py`：追加 3 条 E4 跨包 import 静态规则，未放宽任何 E-2 / E-3 既有规则
- `落地设计文档/19-E4案情录入端分步骤系统提示词文档.md`：E4-5 落地结论补记

## 二、守门覆盖项（10 条，逐条 PASS）

1. intake 公开面 / 端点存在且 gated：`ENABLE_INTAKE` 默认 false；端点 403 降级、不回显 query_text、无 candidate_refs。
2. 产品包互不 import：intake 不 import statute / drafting / casebook，也不被其反向 import。
3. 不深引检索底层：intake 不引 retrieval / rerank / summary / query_processing，只经 `app.kernel.rag` 公开面的 InternalSearchService。
4. 仅 intake 产品包存在：statute / drafting / casebook 仍不存在。
5. 请求体白名单：schema 严格 = SearchProfile 五字段 + mode / limit，`extra=forbid`，正文型 / PII 型键模型层即拒。
6. CandidateRef 白名单：严格七字段、100% 有 source_anchors、0 正文；输出 dump 仅白名单键。
7. 原始案情零上送：intake 可执行代码（剔除 docstring / 注释）无正文 / PII 型数据键；无持久层 import、无落库迹象。
8. AI 增强子开关：`ENABLE_INTAKE_AI_EXTRACTION` 默认 false 且无 on 路径；intake 可执行代码不引该 flag、无服务端 AI / 原始文本接收迹象。
9. 端点边界：include_router = 13（仅 intake 新增）；intake router 仅 1 个端点 POST /api/intake/search；无 /api/internal、/api/ecosystem、其它产品端点。
10. fixture / docs 红线：e4-* 测试 fixture 与 docs 无真实长案情 / 裁判正文 / 真实 PII（最长连续中文 ≤20，阈值 40）。

## 三、测试结果

- CMD1（E4 套件）：`test_e4_intake_boundaries.py + test_e4_intake_api.py + test_e4_intake_sanitize.py` = **90 passed**
- CMD2（E1/E2/E3 回归）：7 个文件 = **172 passed**（host `.venv311` 权威口径）
- E4-1 契约：`test_e4_intake_contracts.py` = **36 passed**
- 新增守门文件单跑：**44 passed**
- E2a 追加后：**13 passed**（原 10，+3 条 E4 规则）

> 计数校正：本文初稿写 CMD2=169 / E2a=10，实为 VM bash 读到 stale mount（只见 10 个 test 函数、缺追加的 3 个 E4 函数、文件 278 行）。host 磁盘文件实含 13 个函数（356 行）；Edit / codex 均作用于 host 磁盘。codex E4-6（`e4-final-verification-20260617-112542.md` §3.2）以 host `.venv311` 实跑为准：E2a=13、CMD2=172、E4 全套=126。以此为权威口径。教训：host→VM mount stale 时 VM bash 的 pytest 计数不可信，须以 host `.venv311` 复跑为准。

E2 / E3 既有边界规则未被放宽（仅追加、未修改既有断言）。

## 四、环境说明

VM 默认缺后端依赖。本步在 VM 内 `pip install --break-system-packages` 安装固定版本（fastapi 0.111.0 / pydantic 2.7.1 / pydantic-settings 2.2.1 / sqlmodel 0.0.18 / SQLAlchemy 2.0.30 / httpx 0.27.0 / pytest 8.2.0）后，用 Python 3.10.12 实跑全绿。建议 E4-6 在 host `.venv311`（Py 3.11.9）复跑，与 E3-5 权威口径一致。

注意（host→VM mount 坑）：host 侧 Write 改测试文件后，VM 内 pytest 可能命中旧的 assertion-rewrite `.pyc`（报错行号偏移 1 行 + 显示 `>  ???`）。处理 recipe：`find . -name __pycache__ -prune -exec rm -rf {} +` + `touch tests/*.py` 后重跑即恢复。

## 五、flag 默认值复核

- `ENABLE_INTAKE` = false
- `ENABLE_INTAKE_AI_EXTRACTION` = false

## 六、是否允许进入下一步

允许进入 **E4-6**。下一步标题：类案检索助手 E4-6 验收与下一步门禁。
