# E6-5 文书工作台 drafting 消费边界与护栏守门 Gate（2026-06-18）

- 步骤：E6-5（消费边界与护栏守门）
- 结论：**CONDITIONAL_GO**（VM 静态守门全过；运行时契约 / TestClient 守门 host `.venv311` 权威；
  既有 E4/E5 陈旧快照断言留 E6-6 host 复核统一上移基线）
- 基线：E6-1~E6-4 全 GO（contract `*-045409` / api `*-053316` / web `*-070017` / export `*-090123`）
- 本步只加守门测试 + append-only 跨包 import 规则，**零业务改动**。

## 新增 / 修改文件

| 文件 | 动作 | 说明 |
| --- | --- | --- |
| `apps/api/tests/test_e6_drafting_boundaries.py` | 新增 | 九类边界守门（静态 AST + 运行时契约，37 test 函数/参数） |
| `apps/api/tests/test_e2a_kernel_boundary.py` | append-only | 追加 6 条 E6 drafting 跨包 import 规则（不放宽 E2/E2a/E3/E4/E5） |
| `docs/development/e6-drafting-gate-20260618-111250.{md,json}` | 新增 | 本守门口径记录 |
| `落地设计文档/21-E6文书工作台分步骤系统提示词文档.md` | 修改 | 补 E6-5 落地小节 |

## 守门覆盖清单（九类，每类 ≥1 条断言）

1. **只组装不起草**：`test_drafting_code_has_no_text_generation_call`、
   `test_drafting_code_has_no_draft_body_or_outcome_tokens`、`test_drafting_assemble_is_sanitize_not_generation`、
   `test_drafting_descriptor_model_has_no_body_or_outcome_fields_runtime`、
   `test_drafting_sanitize_rejects_draft_body_and_outcome_keys_runtime`、`test_drafting_assert_no_draft_body_runtime`。
2. **引用必带锚点**：`test_drafting_candidate_ref_requires_source_anchors_runtime`、
   `test_drafting_sanitize_drops_anchorless_refs_keeps_anchored_runtime`、
   `test_drafting_sanitize_drops_anchorless_statute_refs_runtime`。
3. **持久层零正文**：`test_drafting_persist_model_has_no_body_columns`、
   `test_drafting_store_write_whitelist_and_forbidden_keys`、
   `test_drafting_store_rejects_forbidden_persist_keys_runtime`、`test_drafting_logs_do_not_emit_body_or_note_fulltext`。
4. **导出守门**：`test_drafting_export_forces_disclaimer_header`、`test_drafting_export_does_not_read_article_text`、
   `test_drafting_export_drops_anchorless_refs`、`test_drafting_export_has_forbidden_phrase_guard`、
   `test_drafting_export_executable_code_has_no_body_tokens`。
5. **drafting 不直连检索底层**：`test_drafting_consumes_only_kernel_public_surface`、
   `test_drafting_does_not_deep_import_retrieval_runtime`。
6. **产品包互不 import**：`test_drafting_does_not_import_other_product_packages`、
   `test_drafting_not_imported_by_other_product_packages`、`test_kernel_does_not_import_drafting`。
7. **CandidateRef/StatuteRef 仍零正文**：`test_drafting_candidate_ref_fields_equal_e1_whitelist_runtime`、
   `test_drafting_statute_ref_fields_equal_e5_whitelist_runtime`、`test_drafting_response_view_carries_no_body_fields`。
8. **casebook 仍不存在**：`test_casebook_package_absent`、`test_e6_allowed_product_packages_only`、
   `test_enable_casebook_and_drafting_default_false`、`test_all_product_and_ai_flags_default_false`。
9. **多租户/鉴权**：`test_drafting_default_visibility_private`、`test_drafting_store_enforces_tenant_isolation`、
   `test_drafting_endpoints_require_login_and_gated`、`test_drafting_disabled_returns_403_runtime`。

附：`test_include_router_count_is_15`、`test_no_casebook_endpoint_fragment`、隐私扫描三条
（drafting 可执行代码无 PII/凭据、E6 fixture/docs 无长正文）。

## append-only 规则（test_e2a_kernel_boundary.py）

新增 E6-5 段（不修改、不放宽任何 E2/E2a/E3/E4/E5 既有规则）：

- `test_e6_drafting_does_not_import_other_product_packages`：drafting 不 import intake/statute/casebook。
- `test_e6_drafting_not_imported_by_other_product_packages`：其它产品包不 import drafting。
- `test_e6_kernel_does_not_import_drafting`：内核不反向 import drafting。
- `test_e6_drafting_consumes_only_kernel_public_surface`：drafting 只走 app.kernel 公开面。
- `test_e6_drafting_does_not_deep_import_retrieval_runtime`：drafting 不深引检索运行时。
- `test_e6_casebook_still_forbidden_drafting_allowed`：`E6_FORBIDDEN_PRODUCT_PACKAGES=(casebook,)` 仍禁，
  `E6_ALLOWED_PRODUCT_PACKAGES=(intake,statute,drafting)` 合法放行（drafting 不再越界）。

## 既有越界 / 陈旧断言（留 E6-6 host 复核，本步不擅改业务/不放宽）

均为 **E6-2 合法变更（include_router 14→15 + 新增 drafting 产品包）导致的陈旧快照断言**，
非本守门步引入，亦非 VM 截断假象（host `.venv311` 同样会失败）。按 E5-6→E5-7 既有纪律，
由 E6-6 验收步（host 权威）统一上移基线，**不在本守门步修改既有 E4/E5 断言**（不在允许修改清单）：

| 文件:行 | 陈旧断言 | 现状 | E6-6 处置 |
| --- | --- | --- | --- |
| `tests/test_e4_intake_boundaries.py:583` | `count == 14` | 实际 15 | 上移为 15 |
| `tests/test_e4_intake_boundaries.py:40` | `ALLOWED_PRODUCT_PACKAGES={intake,statute}` | drafting 已存在 | 追加 drafting |
| `tests/test_e5_statute_boundaries.py:676` | `count == 14` | 实际 15 | 上移为 15 |
| `tests/test_e5_statute_boundaries.py:46` | `ALLOWED_PRODUCT_PACKAGES={intake,statute}` | drafting 已存在 | 追加 drafting |

> 说明：E4 `test_e4_intake_boundaries.py` 在 E5-7 已被上移至 14；E6-2 落地 drafting 后再次成为陈旧。
> 本步新增的 `test_e6_drafting_boundaries.test_include_router_count_is_15` 与
> `test_e6_allowed_product_packages_only` 已对当前态（15 + drafting）做出权威断言。

## 隐私扫描结果

- drafting 可执行代码（去 docstring/注释 + 去黑名单常量后）：起草正文 0 / 裁判正文 0 / 结论 0 /
  胜负 0 / PII 0 / 凭据 0（`token` 仅 Bearer 解析上下文，不在凭据黑名单）。
- 导出模块去注释可执行代码：正文型字段 0；`article_text` 读取 0；强制免责头 ≥3 处注入。
- 新增 `test_e6_drafting_boundaries.py` / `test_e2a` 追加段最长连续中文 21 字（< 40 安全边界）。
- E6 docs（e6-drafting-*.md/json）长正文 0。

## 验收对照

- 九类守门均有断言（VM 静态类全过；运行时类 host 权威）。
- `include_router=15`；`app/casebook` 不存在；7 产品 flag + AI 抽取 flag 默认全 false。
- test_e2a append-only 追加 E6 规则，E2~E5 既有规则未放宽。
- 隐私扫描 0 命中（正文/胜负/结论/PII/凭据/禁用文案）。

## 是否允许进入 E6-6

**是（CONDITIONAL_GO）**，条件：E6-6 在 host `.venv311` 复跑 E6 全套 + E4/E5 回归，
并按上表上移 E4/E5 陈旧 router-count/inventory 快照基线。

下一步：类案检索助手 E6-6 E6 验收与下一步门禁。
