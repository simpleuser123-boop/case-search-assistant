# E3-0 入场复核与阻塞清零 · 验收报告

- 步骤：E3-0（E 系列多产品生态 · E-3 检索内部服务接口之入场复核步）
- 时间：2026-06-15T15:33:49
- 比对基线：E-2 末态 `docs/development/e2b-release-gate-20260615-113552.json`
- 结论：**GO — 允许进入 E3-1**
- 性质：纯复核步。不实现 E3 服务契约、不改搜索链路、不新增测试、不修复前端业务文件。

---

## 0. 简报口径 vs 现盘事实（重要）

任务简报称 E-2b gate 为 `CONDITIONAL_GO`，并把 E3 前置阻塞描述为「用户恢复 10 个被截断前端文件并复跑 build 确认 118 modules」。

但**实读** `e2b-release-gate-20260615-113552.json`：`overall = GO`，前端 build parity 已记为 `GO`（118 modules / tsc 0 error / 35 test files passed），`next_step.precondition = 无前置阻塞`。

两者说法不一致。本步不轻信任一方，独立在 VM 现盘上复核：前端文件完整性 + 实跑 build + 后端核心回归，**以现盘事实为准**。复核结果支持「阻塞已清零」。

---

## 1. E-2b 后端 GO + 阻塞定位

`e2b-release-gate` 后端四项结论全 GO（基础搜索可用 / 内核物理迁移 / shim 身份保持无分叉 / 后端行为零变化），前端 parity 也已记为 GO。E-2b 后端共享内核迁移为 **GO**，gate 自身未遗留开放阻塞。本步对前端做独立复跑二次确认。

## 2. 被报告截断的前端文件 — 现盘全部完整（未重写）

逐文件检查行数/字节/收尾语法，**15 个文件全部完整**，均以完整 `}` / `);` 收尾，无半句截断：

| 文件 | 行 | 字节 | 收尾 |
|---|---|---|---|
| src/vite-env.d.ts | 29 | 1414 | ✓ |
| src/config/featureFlags.ts | 135 | 8212 | ✓ |
| src/components/bulkImport/BulkImportPanel.tsx | 198 | 8064 | ✓ |
| src/components/bulkImport/BulkImportPanel.test.tsx | 100 | 3728 | ✓ |
| src/components/permission/PermissionPanel.tsx | 195 | 7047 | ✓ |
| src/components/permission/PermissionPanel.test.tsx | 75 | 2861 | ✓ |
| src/components/sharing/SharingPanel.tsx | 159 | 6023 | ✓ |
| src/components/sharing/SharingPanel.test.tsx | 69 | 2568 | ✓ |
| src/components/team/TeamWorkspacePanel.tsx | 152 | 5605 | ✓ |
| src/components/team/TeamWorkspacePanel.test.tsx | 70 | 2688 | ✓ |
| src/pages/HomePage.tsx | 48 | 1864 | ✓ |
| src/pages/HomePage.test.tsx | 221 | 7839 | ✓ |
| src/pages/SearchPage.test.tsx | 1692 | 59145 | ✓ |
| src/services/billingApi.ts | 244 | 7561 | ✓ |
| src/services/tendencyApi.ts | 142 | 4651 | ✓ |

E-2b 记录的 mount flush 截断阻塞在现盘已清零，**本步未重写任何文件**。

## 3. 前端 build — 恢复 118 modules

- `npm run build`（`tsc -b && vite build`）：**tsc exit 0**，vite **✓ 118 modules transformed**。
- 唯一一次报错在 `emptyDir` 阶段：`EPERM: operation not permitted, unlink dist/assets/*.css` —— 挂载盘拒删既有 `dist`，属**环境写权限限制，非编译/代码失败**（编译与模块转换均已成功）。
- 二次确认：`npx vite build --outDir <fresh outputs dir> --emptyOutDir` → **✓ 118 modules transformed** → 完整写出产物。资源 hash（`index-BQFKuWrM.css` / `index-C2UWiuii.js`）与 E-2b 既有 `dist` **逐字节一致**，证明可复现、118 modules parity 成立。

**build parity = 通过。**

## 4. 后端 E-2b 核心回归 — 80 passed / 0 failed

```
DATABASE_URL=sqlite:///./e3_tmp.db python3 -B -m pytest \
  tests/test_e1_contracts.py tests/test_e2a_kernel_boundary.py tests/test_e2b_shim_equivalence.py
→ 80 passed, exit 0
```

| 文件 | passed |
|---|---|
| test_e1_contracts.py | 24 |
| test_e2a_kernel_boundary.py | 7 |
| test_e2b_shim_equivalence.py | 49 |
| **合计** | **80** |

24+7+49=80，与 `e2b-release-gate` 记录逐位一致，无新增失败。

> 环境说明：VM 内 `.venv311`/`.venv` 均为 Windows 格式不可用；用 system Python 3.10 重装 `requirements.txt` 全量依赖（含 chromadb 0.5.0），并将 numpy 降级至 1.26.4 以兼容 chromadb（`np.float_` 在 numpy 2.0 被移除）。仅为复核装依赖，未改任何源码。

## 5. 无产品包

`app/intake`、`app/statute`、`app/drafting`、`app/casebook` **均不存在**。`app/` 顶层目录仅 M1~M5 + E-2 内核既有包（account/api/billing/bulk_import/case_store/contracts/core/eval/kernel/permission/pipeline/query_processing/rerank/retrieval/sharing/summary/team/tendency_analysis/tendency_gate）。

## 6. 无 E3 端点

- 无 `internal/search` 端点；无 SearchProfile/CandidateRef 端点接线；contracts 模块未接线任何端点。
- `main.py` 的 `include_router` 共 **12 个**，全部为 M1~M5 既有路由（health/search/cases/events/feedback/auth/team/permission/sharing/bulk_import/tendency/billing）。
- **计数口径说明**：E-2b 报告称「10 router 不变」，实测为 12（含 M5 的 tendency/billing，E 系列之前即存在）。此为 E-2b 自身计数口径出入，**与 E3-0 无关**——E3-0 只校验有无【新增 E3 端点】，结论为「无」。

## 7. flags 默认全 false

`config.py` 中全部业务 flag 默认 `false`，`ENABLE_WEIGHTED_RERANK` 仍为 `false`；`grep 'bool = True'` 无命中。涵盖 M4（QUERY_REWRITE/SUMMARY/EXPANDED_SEARCH）、M5（ACCOUNT_SYSTEM/TEAM_WORKSPACE/PERMISSION_TIERING/TEAM_SHARING/TENDENCY_ANALYSIS/BILLING）、E-1（ECOSYSTEM/INTAKE/STATUTE_SEARCH/DRAFTING/CASEBOOK）。

## 8. 范围合规（未越界）

- 未修复任何截断前端文件 ✓
- 未实现 E3-1/E3-2 ✓
- 未修改 `app/api/search.py`（mtime 2026-06-15T09:46，本会话之前）✓
- 未修改 kernel（`kernel/__init__.py` mtime 2026-06-15T11:07，本会话之前）✓
- 未修改 schemas ✓
- 唯一副作用：为跑后端回归装了 Python 依赖；前端 build 产物写到 `outputs/` 临时目录，未污染仓库 `dist`。

---

## 9. 验证命令与结果汇总

| # | 命令 | 结果 |
|---|---|---|
| 1 | 15 文件 `wc -l/-c` + `tail` 收尾检查 | 全部完整，无截断 |
| 2 | `npm run build` | tsc 0 error，118 modules；EPERM 仅挂载盘拒删 dist |
| 3 | `npx vite build --outDir <fresh> --emptyOutDir` | 118 modules，产物 hash 与 E-2b dist 一致 |
| 4 | `pytest test_e1_contracts test_e2a_kernel_boundary test_e2b_shim_equivalence` | **80 passed / 0 failed / exit 0** |
| 5 | `ls app/{intake,statute,drafting,casebook}` | 全 absent |
| 6 | `grep -rnE 'internal/search\|SearchProfile\|CandidateRef' app/api app/main.py` | 无接线；include_router=12 全为既有 |
| 7 | `grep -nE 'ENABLE_*' app/core/config.py` / `grep 'bool = True'` | 全 false / none |

---

## 10. E3 入场结论

| 复核项 | 结论 |
|---|---|
| 前端 build parity 阻塞清零 | **GO**（118 modules，tsc 0 error，15 文件完整） |
| 后端 E-2b 核心回归 | **GO**（80 passed / 0 failed） |
| 无越界（产品包/端点/flag/搜索链路） | **GO** |
| **E3-0 总判定** | **GO** |

- **阻塞项**：无。
- **是否允许进入 E3-1**：**允许**。

E-2b 唯一前置阻塞（前端 build parity / 截断文件恢复）在现盘已清零；后端 E-2b 核心回归全绿；无任何 E3/产品越界；全部 flag 默认 false。E-3 具备入场条件。

**下一步标题：类案检索助手 E3-1 内部服务契约冻结**
（行为零变化比对基线仍为 `e2b-release-gate-20260615-113552.json`，E-2 末态。）

## 11. 产物

- `docs/development/e3-entry-check-20260615-153349.md`（本报告）
- `docs/development/e3-entry-check-20260615-153349.json`
