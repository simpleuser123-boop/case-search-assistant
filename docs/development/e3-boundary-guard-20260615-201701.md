# E3-4 消费边界与护栏守门 · 验收报告

- 步骤：E3-4（E 系列多产品生态 · E-3 检索内部服务接口之「消费边界与护栏守门」）
- 时间：20260615-201701
- 比对基线：E3-3 `docs/development/e3-search-api-parity-20260615-190316.json` = GO
- 上游基线：E-2b 末态 `docs/development/e2b-release-gate-20260615-113552.json`
- 结论：GO — 允许进入 E3-5
- 性质：只新增守门测试 + 文档。不改 InternalSearchService 行为、不注册端点、不新建产品包、不改排序/召回/summary 策略。外部 /api/search 行为零变化。

---

## 1. 本步交付的 E3 子目标

把 E3 的架构纪律固化成可执行测试，避免后续 E4~E7 绕开内部检索服务或泄露正文：
内部检索服务（InternalSearchService / 公开服务面）成为后续产品唯一允许消费的检索面；
CandidateRef 严格受 E-1 白名单约束、只携带元数据 + 来源锚点，零正文。

## 2. 新增 / 修改文件

| 文件 | 类型 | 说明 |
|---|---|---|
| `apps/api/tests/test_e3_internal_search_boundaries.py` | 新增 | E3-4 守门测试，22 passed，覆盖守门 1~8。 |
| `apps/api/tests/test_e2a_kernel_boundary.py` | 修改（append-only） | 在 E-2 既有 7 条规则之后追加 3 条 E3 边界规则，不放宽任何既有 E-2 规则。 |
| `docs/development/e3-boundary-guard-20260615-201701.md` | 新增 | 本报告。 |
| `docs/development/e3-boundary-guard-20260615-201701.json` | 新增 | 机器可读 gate。 |

## 3. 守门覆盖项（提示词 1~8 逐条）

| # | 守门 | 实现 | 结论 |
|---|---|---|---|
| 1 | E3 内部服务公开面存在 | 从 `app.kernel.rag` / `app.kernel` 可导入 InternalSearchService / SearchProfile / CandidateRef 等 12 个稳定符号，且与底层身份一致（re-export 非复制） | GO |
| 2 | 服务模块不得 import 产品包 | AST 扫描 internal_search_service.py / internal_search_contracts.py 无 intake/statute/drafting/casebook import | GO |
| 3 | 产品包仍不得存在 | `app/{intake,statute,drafting,casebook}` 全缺席 | GO |
| 4 | api/ 不新增内核深引；/api/search 经内部服务执行 | search.py 仅经 app.kernel 公开面消费；引用 InternalSearchService 且调用 `.execute(`；不再内联 merge_case_candidates / split_low_confidence_candidates；其余 api/ 文件不得直引检索执行原语 | GO |
| 5 | 不新增 E3 对外端点 | 全 api/ 扫描无 `/api/internal/*` `/api/ecosystem/*`；include_router 仍 12；契约/服务模块不接线 HTTP | GO |
| 6 | CandidateRef 严格等于 E-1 白名单子集 | model_fields == 白名单 7 字段；extra=forbid 拒绝 summary/highlights/matched_text/metadata/text/content/body；model_dump 键集严格等于白名单 | GO |
| 7 | CandidateRef 必须有元数据型 source_anchors | 空/缺锚点 fail-closed；SourceAnchorRef 仅 case_id/source_chunk_id/anchor_type；锚点 extra=forbid 拒正文；缺 case_id/source_chunk_id 候选被拒 | GO |
| 8 | fixture / docs 不得写长正文 | test_e3_*.py 与 docs/development/e3-*.{md,json} 最长连续中文 <= 阈值 40（实测 fixture 13、docs 19） | GO |

## 4. 验证命令与结果

```bash
cd apps/api
# 环境：全新 VM，system Python 3.10 + pip 安装 fastapi/pydantic/sqlmodel/pytest/httpx；DATABASE_URL=sqlite 覆盖。
pytest tests/test_e1_contracts.py tests/test_e2a_kernel_boundary.py tests/test_e2b_shim_equivalence.py \
       tests/test_e3_internal_search_contracts.py tests/test_e3_internal_search_service.py \
       tests/test_e3_search_api_parity.py tests/test_e3_internal_search_boundaries.py
# => 169 passed

# 搜索链路回归（证明零行为变化）
pytest tests/test_search_api_fallback_smoke.py tests/test_feature_flag_rollback.py tests/test_health.py \
       tests/test_summary_service.py tests/test_performance_smoke.py \
       tests/test_m1_3_candidate_comparison.py tests/test_m1_3_legal_candidate_robustness.py
# => 60 passed
```

- 新增 E3 边界文件：22 passed。
- E-2a 守门追加 E3 规则后：10 passed（原 7 + 新 3，未放宽既有规则）。

## 5. 是否改变外部 /api/search 行为

否。本步只新增守门测试与文档，未触碰 search.py / 内核服务 / schemas / 排序 / 召回 / summary。

## 6. 边界合规

| 项 | 结论 |
|---|---|
| 新增 HTTP 端点 | 无（include_router 仍 12） |
| 新建产品包 | 无（四者均 absent） |
| 新增 /api/internal/search、/api/ecosystem/search | 无 |
| 深引内核私有实现 | 否（守门断言） |
| flag 默认值变化 | 无（5 个 flag 仍 false） |
| E-2 既有规则被放宽 | 否（append-only） |

## 7. 隐私 / 正文扫描

- fixture / docs 最长连续中文 <= 阈值 40，无长案情 / 裁判正文。
- 所有新文件 0 null 字节（已校验，规避 host→VM flush 截断坑）。
- CandidateRef / SourceAnchorRef 守门确认零正文型字段，extra=forbid fail-closed。

## 8. E3-4 结论

| 门禁 | 结论 |
|---|---|
| public_face_gate（内部服务公开面齐备 + 身份一致） | GO |
| import_boundary_gate（无产品包深引 / 无内核深引绕过） | GO |
| endpoint_gate（无新增 E3 对外端点 / include_router=12） | GO |
| whitelist_gate（CandidateRef 严格等于 E-1 子集） | GO |
| anchor_gate（必有元数据型 source_anchors / fail-closed） | GO |
| body_leak_gate（fixture/docs 无长正文） | GO |
| single_authority_gate（内部服务为唯一检索消费面） | GO |
| regression_gate（169 + 60 passed，零行为变化） | GO |
| e2_not_relaxed_gate（E-2 既有规则未放宽） | GO |
| **E3-4 总判定** | **GO** |

- 是否允许进入 E3-5：允许。
- 下一步新会话标题：类案检索助手 E3-5 验收与下一步门禁
